import { ToogleFullscreenDirective } from './toogle-fullscreen.directive';

describe('ToogleFullscreenDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleFullscreenDirective();
    expect(directive).toBeTruthy();
  });
});
